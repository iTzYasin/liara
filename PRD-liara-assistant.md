# PRD دستیار هوشمند مستندات لیارا

## ۱. اطلاعات سند

| مورد | مقدار |
|---|---|
| نام موقت محصول | دستیار لیارا |
| نسخه سند | ۱.۰ |
| وضعیت | آماده پیاده‌سازی |
| تاریخ | ۱۴۰۵/۰۵/۲۹ |
| زبان محصول | فارسی، راست‌به‌چپ |
| مدل مولد | Gemini 3.5 Flash-Lite |
| منبع دانش | فقط مستندات رسمی لیارا و داده ارسالی خود کاربر |
| مالک استقرار | تیم پروژه |

## ۲. خلاصه اجرایی

لیارا با رشد تعداد سرویس‌ها، با حجم رو‌به‌افزایش مستندات و تیکت‌هایی مواجه است که پاسخ آن‌ها در مستندات وجود دارد، اما کاربر نتوانسته سند مناسب را پیدا کند، آن را بفهمد یا به اقدام عملی تبدیل کند.

محصول پیشنهادی یک چت‌بات عمومی نیست؛ یک دستیار مستندمحور و عامل‌محور فارسی است که:

- نیت و سطح فنی هر پیام را مستقل از پیام‌های قبلی تشخیص می‌دهد.
- پاسخ را فقط بر شواهد مستندات رسمی لیارا متکی می‌کند.
- لاگ، کد، تنظیمات، تصویر خطا و فایل کاربر را تحلیل می‌کند.
- در مسئله مبهم، به‌جای حدس‌زدن سؤال تکمیلی هدفمند می‌پرسد.
- عیب‌یابی را به‌صورت چندمرحله‌ای تا رسیدن به نتیجه ادامه می‌دهد.
- برای هر ادعای مربوط به لیارا، منبع دقیق و قابل بازشدن نمایش می‌دهد.
- در نبود شواهد کافی، پاسخ ساختگی تولید نمی‌کند و یک تیکت آماده ارسال می‌سازد.
- اطلاعات حساس کاربر را پیش از پردازش و ذخیره‌سازی ماسک می‌کند.

محصول به حساب، سرویس‌ها، کنسول یا لاگ‌های لیارا دسترسی ندارد و هیچ عملیاتی را در زیرساخت کاربر اجرا نمی‌کند. تنها داده بیرونی آن، محتوایی است که خود کاربر آگاهانه در گفتگو وارد می‌کند.

## ۳. ورودی‌ها و شواهد محصول

### ۳.۱ منابع اولیه

- شرح مسئله در فایل liara-video-text.txt
- الزامات و معیارهای داوری در فایل liara-req.txt
- مستندات رسمی: https://docs.liara.ir/
- مخزن رسمی مستندات: https://github.com/liara-cloud/docs
- Snapshot محلی مخزن با commit برابر 31f2ef7

### ۳.۲ وضعیت corpus

مخزن رسمی دارای یک corpus آماده مصرف LLM در مسیر public/llms است. در Snapshot بررسی‌شده حدود ۱۱۴۲ فایل Markdown وجود دارد:

| حوزه | تعداد تقریبی فایل |
|---|---:|
| PaaS | ۴۲۵ |
| One Click Apps | ۱۸۹ |
| DBaaS | ۱۶۷ |
| AIaaS | ۱۲۷ |
| IaaS | ۶۶ |
| References | ۶۵ |
| Email Server | ۳۸ |
| Object Storage | ۳۴ |
| Mirrors | ۲۱ |
| DNS | ۸ |
| Overview | ۲ |

هر فایل LLM شامل لینک صفحه اصلی است؛ بنابراین عنوان، بخش، URL و متن سند می‌توانند بدون scraping شکننده وارد ایندکس شوند.

### ۳.۳ محدودیت داده

هیچ داده واقعی از تیکت‌ها، حساب‌ها یا رخدادهای کاربران در دسترس نیست. ارزیابی محصول باید با مجموعه آزمون مصنوعی اما قابل‌ردیابی به مستندات انجام شود.

## ۴. مسئله محصول

### ۴.۱ مسئله کاربر

کاربر لیارا معمولاً یکی از این موانع را تجربه می‌کند:

- عبارت مناسب برای جستجو را نمی‌داند.
- نمی‌داند مسئله مربوط به کدام سرویس یا بخش مستندات است.
- سند را پیدا می‌کند اما نمی‌تواند آن را به وضعیت پروژه خود تطبیق دهد.
- میان چند صفحه مرتبط، ترتیب انجام مراحل را تشخیص نمی‌دهد.
- خطا یا لاگ دارد، اما نمی‌داند کدام بخش آن برای عیب‌یابی مهم است.
- از ترس اتلاف زمان، مستقیماً تیکت ثبت می‌کند.

### ۴.۲ مسئله کسب‌وکار

- بار پشتیبانی برای پرسش‌های مستندشده بالا می‌رود.
- زمان رسیدن کاربر به نتیجه طولانی می‌شود.
- ارزش مستندات موجود به‌طور کامل محقق نمی‌شود.
- پاسخ غیرمستند یک دستیار هوشمند می‌تواند اعتماد به لیارا را کاهش دهد.

## ۵. اهداف و غیرهدف‌ها

### ۵.۱ اهداف

1. رساندن کاربر از سؤال یا خطا به یک پاسخ قابل‌اقدام و مستند.
2. کاهش احتمال مراجعه به پشتیبانی برای مسائل پاسخ‌پذیر از روی داک.
3. حفظ کیفیت پاسخ در سؤال‌های ساده، پیچیده و چندمرحله‌ای.
4. ایجاد تجربه مکالمه‌ای ممتاز، فارسی، Responsive و مناسب محتوای فنی.
5. نمایش شفاف منبع و مرز دانش محصول.
6. ارائه قابلیت‌های Agentic واقعی بدون ادعای دسترسی به زیرساخت کاربر.
7. کنترل هزینه، Token، نرخ درخواست و خطاهای سرویس مدل.
8. فراهم‌کردن شواهد قابل‌نمایش برای همه معیارهای داوری.

### ۵.۲ غیرهدف‌ها

- ورود به حساب لیارا یا مشاهده سرویس‌های کاربر
- دریافت خودکار لاگ از کنسول یا سرور
- اجرای دستور، Deploy، حذف یا تغییر منابع
- ارسال واقعی تیکت به پشتیبانی
- پاسخ آزاد درباره موضوعات نامرتبط با لیارا
- جایگزینی کامل تیم پشتیبانی
- ذخیره دائمی ورودی خام یا Secret کاربر

## ۶. کاربران و Jobs to be Done

### ۶.۱ کاربر اصلی

مشتری یا توسعه‌دهنده‌ای که از یکی از سرویس‌های لیارا استفاده می‌کند و برای راه‌اندازی، پیکربندی یا رفع خطا به کمک نیاز دارد.

سطح فنی کاربر می‌تواند در یک مکالمه تغییر کند؛ حتی ممکن است ادامه گفتگو را فرد دیگری انجام دهد. بنابراین سطح تخصص، ویژگی ثابت کاربر یا Conversation نیست.

### ۶.۲ رفتار تطبیقی سطح تخصص

- سطح فنی در هر پیام به‌صورت داخلی استنباط می‌شود.
- نتیجه تشخیص به کاربر نمایش داده نمی‌شود.
- Context مسئله و اقدامات قبلی حفظ می‌شود.
- طول پاسخ، میزان توضیح اصطلاحات و جزئیات کد با پیام جاری تطبیق می‌یابد.
- درخواست صریح «ساده‌تر بگو» یا «فنی‌تر توضیح بده» بر تشخیص خودکار اولویت دارد.

### ۶.۳ نیازهای اصلی

- «صفحه درست مستندات را برای مسئله‌ام پیدا کن.»
- «این خطا را با توجه به لیارا تحلیل کن.»
- «مراحل را به ترتیب و متناسب با پروژه من بگو.»
- «کد یا تنظیمات قابل‌کپی تولید کن.»
- «بگو کدام بخش پاسخ از داک لیاراست و کدام بخش دانش عمومی است.»
- «اگر حل نشد، اطلاعات لازم برای یک تیکت خوب را آماده کن.»

### ۶.۴ ذی‌نفع ثانویه

داور چالش که صحت، UX، Agentic بودن، امنیت، پایداری، استقرار و هزینه را ارزیابی می‌کند. داور، پرسونای اصلی محصول نیست.

## ۷. اصول تجربه محصول

1. **شاهد قبل از پاسخ:** بازیابی منبع پیش از تولید پاسخ انجام می‌شود.
2. **یک ورودی واحد:** کاربر مجبور به انتخاب حالت «جستجو»، «چت» یا «عیب‌یابی» نیست؛ Intent خودکار تشخیص داده می‌شود.
3. **کمترین سؤال لازم:** فقط وقتی پاسخ بدون اطلاعات بیشتر نامطمئن است، یک سؤال با بیشترین ارزش اطلاعاتی پرسیده می‌شود.
4. **اقدام‌پذیری:** پاسخ به دستور، چک‌لیست، کد یا قدم بعدی ختم می‌شود.
5. **نمایش مرز دانش:** ادعای لیارایی، راهنمای عمومی و فرض مدل از هم جدا هستند.
6. **کنترل در دست کاربر:** هیچ اقدام بیرونی بدون حضور و اجرای خود کاربر رخ نمی‌دهد.
7. **حریم خصوصی پیش‌فرض:** داده خام حساس نگهداری نمی‌شود.
8. **Progressive disclosure:** ابتدا نتیجه کوتاه، سپس مراحل، جزئیات و منابع.
9. **عدم جابه‌جایی ناخواسته:** رابط هنگام خواندن تاریخچه، کاربر را به انتهای گفتگو پرتاب نمی‌کند.

## ۸. دامنه قابلیت‌های اصلی

### ۸.۱ پرسش و پاسخ مستندمحور

کاربر سؤال طبیعی می‌پرسد. سیستم Intent، سرویس و واژه‌های کلیدی را استخراج می‌کند، اسناد مرتبط را می‌یابد و پاسخ فارسی با citation تولید می‌کند.

خروجی مورد انتظار:

- پاسخ مستقیم در ابتدای پیام
- مراحل شماره‌گذاری‌شده در صورت عملیاتی بودن سؤال
- کد یا دستور قابل‌کپی
- هشدارهای مرتبط
- منابع درون‌متنی و فهرست منابع
- قدم بعدی پیشنهادی

### ۸.۲ عیب‌یابی هدایت‌شده

کاربر خطا، لاگ، کد یا توضیح وضعیت را می‌فرستد. عامل:

1. Secretها را ماسک می‌کند.
2. سرویس، تکنولوژی، مرحله عملیات و نشانه خطا را تشخیص می‌دهد.
3. علت‌های محتمل را فقط بر اساس شواهد رتبه‌بندی می‌کند.
4. در صورت نیاز، یک سؤال تکمیلی می‌پرسد.
5. یک اقدام کم‌ریسک برای بررسی یا رفع پیشنهاد می‌دهد.
6. نتیجه اقدام را از کاربر می‌گیرد و مسیر را ادامه می‌دهد.
7. پس از حل، خلاصه علت و راه‌حل را ارائه می‌کند.
8. در صورت شکست، بسته Escalation تولید می‌کند.

عامل نباید همه چک‌لیست‌های ممکن را یک‌جا روی کاربر تخلیه کند.

### ۸.۳ راه‌اندازی و پیکربندی

برای Intentهایی مانند Deploy، اتصال دامنه، متغیر محیطی، اتصال دیتابیس، Backup، Object Storage و Email، عامل یک برنامه مرحله‌ای می‌سازد و هر مرحله را با وضعیت «انجام نشده»، «در حال بررسی» و «تأییدشده توسط کاربر» دنبال می‌کند.

### ۸.۴ تولید Artifact

عامل می‌تواند Artifact متنی تولید کند، از جمله:

- دستورهای CLI
- نمونه متغیرهای محیطی
- فایل تنظیمات
- قطعه‌کد اتصال
- چک‌لیست Deploy
- متن آماده تیکت

هر Artifact باید:

- عنوان یا نام فایل پیشنهادی داشته باشد.
- LTR و syntax-highlighted نمایش داده شود.
- دکمه Copy داشته باشد.
- Secret واقعی تولید یا بازنمایی نکند.
- وابستگی خود به نسخه، سرویس یا فرض را اعلام کند.
- منبع مرتبط داشته باشد.

### ۸.۵ ورودی چندرسانه‌ای

کاربر می‌تواند متن را Paste کند یا فایل بیفزاید:

- متن، لاگ و Markdown
- فایل‌های کد رایج
- JSON، YAML، ENV، TOML، INI و فایل‌های تنظیمات
- PDF
- تصویر PNG، JPEG و WebP از خطا یا کنسول

قواعد:

- پیش از ارسال، نوع و اندازه فایل اعتبارسنجی شود.
- حداکثر ۳ فایل در هر پیام و حداکثر مجموع ۱۰ مگابایت.
- محتوای متنی بزرگ به بخش‌های مرتبط تقلیل داده شود.
- فایل هرگز اجرا نشود.
- پیش‌نمایش، حذف فایل و وضعیت پردازش در Composer دیده شود.
- تصویر یا PDF فقط وقتی وارد Context مدل شود که برای Intent جاری لازم است.

### ۸.۶ Escalation به پشتیبانی

اگر شواهد کافی وجود نداشت، پاسخ‌ها متناقض بودند یا مسیر عیب‌یابی به نتیجه نرسید، سیستم یک بسته آماده تیکت می‌سازد:

- عنوان پیشنهادی
- سرویس و محیط احتمالی
- شرح کوتاه مسئله
- رفتار مورد انتظار و رفتار مشاهده‌شده
- زمان یا مرحله وقوع
- خطای پاک‌سازی‌شده
- اقدام‌های انجام‌شده و نتیجه هرکدام
- صفحات مستندات بررسی‌شده
- اطلاعاتی که هنوز لازم است

دکمه‌های «کپی متن تیکت» و «بازکردن صفحه ثبت تیکت» نمایش داده می‌شوند؛ ارسال واقعی انجام نمی‌شود.

## ۹. سیاست دانش و Grounding

### ۹.۱ ادعاهای مربوط به لیارا

- فقط اسناد رسمی موجود در corpus مجازند.
- هر ادعای قابل‌بررسی باید Citation داشته باشد.
- مدل اجازه ساختن نام سرویس، محدودیت، نسخه، دستور یا URL را ندارد.
- اگر سند کافی نیست، پاسخ باید ابهام را اعلام کند.

### ۹.۲ دانش عمومی فنی

مدل می‌تواند برای توضیح مفاهیم عمومی یا تحلیل کد از دانش عمومی استفاده کند، مشروط به اینکه:

- بخش با برچسب «راهنمای عمومی خارج از مستندات لیارا» نمایش داده شود.
- با واقعیت مستندشده لیارا تعارض نداشته باشد.
- به‌عنوان رفتار قطعی پلتفرم لیارا بیان نشود.
- Confidence آن از بخش مستندمحور جدا باشد.

### ۹.۳ آستانه اطمینان

| سطح شواهد | رفتار |
|---|---|
| بالا | پاسخ مستقیم همراه Citation و قدم بعدی |
| متوسط | پاسخ مشروط، بیان فرض و سؤال تکمیلی |
| پایین | عدم حدس، نمایش منابع نزدیک و ساخت Escalation |

### ۹.۴ دفاع در برابر Prompt Injection

- متن فایل و مستندات به‌عنوان «داده غیرقابل‌اعتماد» وارد Prompt می‌شود.
- دستورهای موجود در فایل کاربر یا Markdown اسناد حق تغییر System Policy را ندارند.
- مدل فقط Citation IDهای ارائه‌شده توسط Retriever را می‌تواند استفاده کند.
- URL دلخواه کاربر fetch نمی‌شود.

## ۱۰. جریان عامل

    پیام یا فایل کاربر
            |
            v
    اعتبارسنجی و ماسک Secret
            |
            v
    تشخیص Intent، سرویس، سطح فنی و پیچیدگی
            |
            +---- اطلاعات ناکافی؟ ---- بله ----> یک سؤال تکمیلی
            |                               |
            |                               +----> بازگشت به تحلیل
            v
    بازیابی و رتبه‌بندی اسناد
            |
            +---- شواهد پایین؟ ---- بله ----> عدم حدس + Escalation
            |
            v
    تولید پاسخ ساختاریافته
            |
            v
    اعتبارسنجی Citation و سیاست دانش
            |
            v
    Stream پاسخ + منابع + قدم بعدی

### ۱۰.۱ Intentهای پشتیبانی‌شده

- پرسش دانشی مستقیم
- یافتن صفحه یا قابلیت
- راه‌اندازی مرحله‌ای
- عیب‌یابی
- تحلیل کد و تنظیمات
- مقایسه گزینه‌های مستندشده
- تولید Artifact
- پیگیری پاسخ قبلی
- درخواست ساده‌سازی یا توضیح فنی‌تر
- درخواست خارج از دامنه
- Escalation

## ۱۱. قرارداد داخلی پاسخ عامل

خروجی مدل باید Structured و قابل‌اعتبارسنجی باشد و حداقل این فیلدها را داشته باشد:

| فیلد | توضیح |
|---|---|
| intent | Intent تشخیص‌داده‌شده |
| service | حوزه یا سرویس لیارا |
| expertise_hint | سیگنال داخلی سطح فنی؛ هرگز در UI نمایش داده نمی‌شود |
| needs_clarification | آیا پاسخ به سؤال تکمیلی نیاز دارد |
| clarification_question | حداکثر یک سؤال |
| answer_summary | پاسخ کوتاه آغازین |
| steps | مراحل اقدام |
| code_blocks | Artifactهای فنی |
| citations | شناسه منابع بازیابی‌شده |
| general_guidance | بخش عمومی با برچسب مجزا |
| assumptions | فرض‌های ضروری |
| confidence | high، medium یا low |
| next_actions | حداکثر ۳ پیشنهاد مرتبط |
| escalation | داده ساخت بسته تیکت |

Schema در سرور اعتبارسنجی می‌شود. در صورت خروجی نامعتبر، فقط یک Repair attempt مجاز است.

## ۱۲. تجربه کاربری و طراحی رابط

### ۱۲.۱ جهت بصری

- رابط فارسی و RTL
- کد، لاگ، URL، نام فایل و CLI به‌صورت LTR
- استفاده از زبان بصری مستندات لیارا: سطوح خنثی، پس‌زمینه روشن نزدیک به #fbfbfb، متن #181818 و Accent آبی نزدیک به #0076ff
- پشتیبانی Light و Dark
- استفاده از Yekan Bakh در صورت مجاز بودن لایسنس؛ در غیر این صورت Vazirmatn
- حداقل Contrast مطابق WCAG AA
- پرهیز از حباب‌های شلوغ، Gradientهای نمایشی و Animationهای غیرضروری

### ۱۲.۲ معماری اطلاعات Desktop

    +----------------+---------------------------+------------------+
    | تاریخچه گفتگو  | جریان مکالمه              | پنل منابع        |
    | گفتگوی جدید    | پاسخ، کد و اقدام بعدی     | عنوان، بخش، لینک |
    | حذف/تغییر نام  | Composer ثابت در پایین    | پیش‌نمایش سند    |
    +----------------+---------------------------+------------------+

- Sidebar تاریخچه: حدود ۲۶۰ تا ۲۸۰ پیکسل
- ستون مکالمه: عرض خوانایی حداکثر ۸۲۰ پیکسل
- پنل منبع: حدود ۳۴۰ تا ۳۸۰ پیکسل و فقط هنگام نیاز باز

### ۱۲.۳ معماری Mobile

- Header فشرده با دکمه تاریخچه و گفتگوی جدید
- مکالمه تمام‌عرض
- Composer چسبیده به پایین با رعایت Safe Area
- منابع در Bottom Sheet
- تاریخچه در Drawer
- کد دارای اسکرول افقی و دکمه Copy ثابت

### ۱۲.۴ Empty State

- تیتر: «چه مشکلی در لیارا داری؟»
- توضیح کوتاه درباره امکان ارسال سؤال، لاگ یا تصویر خطا
- چهار Prompt پیشنهادی واقعی از حوزه‌های پرکاربرد
- CTA افزودن فایل
- پیام اعتماد: «پاسخ‌ها با منبع مستندات رسمی نمایش داده می‌شوند.»

### ۱۲.۵ ساختار پاسخ در UI

1. نتیجه یا جمع‌بندی یک تا سه خطی
2. سؤال تکمیلی، فقط در صورت نیاز
3. مراحل یا تحلیل
4. کد و Artifact
5. فرض‌ها یا برچسب راهنمای عمومی
6. منابع
7. پیشنهاد قدم بعدی
8. Copy، پسندیدن، نپسندیدن و بازتولید

### ۱۲.۶ تجربه Streaming

- وضعیت‌های کوتاه و واقعی مانند «در حال یافتن مستندات» و «در حال بررسی لاگ» نمایش داده شوند.
- Chain of Thought داخلی نمایش داده نشود.
- کاربر بتواند تولید را متوقف و دوباره اجرا کند.
- پاسخ ناقص پس از قطع شبکه با برچسب مناسب باقی بماند.
- Composer هنگام ارسال غیرفعال مطلق نشود؛ کاربر بتواند متن پیام بعدی را آماده کند.

### ۱۲.۷ رفتار Scroll

- اگر کاربر نزدیک انتهای گفتگو است، پاسخ Streaming به‌طور خودکار دنبال شود.
- اگر کاربر برای خواندن پیام قبلی بالا رفته است، موقعیت Scroll حفظ شود.
- در حالت دوم، دکمه شناور «رفتن به جدیدترین پیام» با شمارنده پیام جدید نمایش داده شود.
- با ارسال پیام توسط خود کاربر، صفحه به پیام ارسالی و پاسخ جدید منتقل شود.
- بازشدن پنل منابع نباید موقعیت Scroll مکالمه را تغییر دهد.

### ۱۲.۸ نمایش منابع شبیه ChatGPT

- Citation درون متن بلافاصله بعد از ادعای مربوطه قرار گیرد.
- Citation به‌شکل شماره یا Chip فشرده نمایش داده شود.
- کلیک روی Citation، پنل منبع را روی همان سند و Heading باز کند.
- کارت منبع شامل عنوان صفحه، Breadcrumb سرویس، Heading، Snippet و لینک اصلی باشد.
- منابع تکراری ادغام شوند.
- منابع استفاده‌نشده در پاسخ نمایش داده نشوند.
- در Mobile، Citation یک Bottom Sheet باز کند.
- لینک همیشه به صفحه رسمی docs.liara.ir برود، نه فایل محلی یا GitHub.

### ۱۲.۹ تاریخچه گفتگو

- مکالمه پاک‌سازی‌شده در IndexedDB همان دستگاه ذخیره شود.
- پس از Refresh در دسترس باشد.
- عنوان گفتگو از اولین مسئله تولید شود و قابل ویرایش باشد.
- حذف یک گفتگو و حذف همه گفتگوها ممکن باشد.
- بک‌اند متن خام مکالمه را به‌صورت دائمی نگه ندارد.
- Sync بین دستگاه‌ها و Login خارج از دامنه فعلی است.

## ۱۳. الزامات عملکردی

| شناسه | الزام | اولویت |
|---|---|---|
| FR-01 | ایجاد، ادامه، تغییر نام و حذف گفتگو | P0 |
| FR-02 | پاسخ Streaming فارسی و حفظ Context | P0 |
| FR-03 | تشخیص Intent و سطح فنی برای هر پیام | P0 |
| FR-04 | پرسیدن حداکثر یک سؤال تکمیلی هدفمند | P0 |
| FR-05 | بازیابی مستندات و Citation درون‌متنی | P0 |
| FR-06 | پنل منبع با لینک و Snippet دقیق | P0 |
| FR-07 | Paste و آپلود متن، کد، تنظیمات، تصویر و PDF | P0 |
| FR-08 | ماسک Secret در Client و Server | P0 |
| FR-09 | عیب‌یابی چندمرحله‌ای و ثبت اقدام‌های قبلی | P0 |
| FR-10 | تولید کد، دستور، Checklist و فایل تنظیمات | P0 |
| FR-11 | پیشنهاد حداکثر سه قدم بعدی مرتبط | P0 |
| FR-12 | ساخت بسته Escalation | P0 |
| FR-13 | تاریخچه محلی پاک‌سازی‌شده | P0 |
| FR-14 | رفتار Scroll تطبیقی | P0 |
| FR-15 | Responsive در Mobile، Tablet و Desktop | P0 |
| FR-16 | Copy کد، متن پاسخ و تیکت | P0 |
| FR-17 | پسندیدن و نپسندیدن پاسخ با دلیل اختیاری | P1 |
| FR-18 | نمایش زمان آخرین به‌روزرسانی ایندکس | P1 |
| FR-19 | جستجو در تاریخچه محلی | P1 |
| FR-20 | Export مکالمه پاک‌سازی‌شده | P1 |

## ۱۴. معماری پیشنهادی

### ۱۴.۱ اجزا

    Browser
      |-- UI فارسی، IndexedDB، ماسک اولیه، Attachment preview
      |
      v
    Chat API / Orchestrator
      |-- Rate limit
      |-- ماسک ثانویه
      |-- Intent و Agent state
      |-- Token budget
      |
      +------> Retriever ------> Meilisearch Index
      |                            ^
      |                            |
      |                       Docs Ingestion
      |
      +------> Gemini 3.5 Flash-Lite
      |
      +------> Citation/Policy Validator
      |
      +------> SSE Stream to UI

### ۱۴.۲ انتخاب فنی پیشنهادی

- Frontend و BFF: Next.js با TypeScript
- UI: Tailwind CSS و کامپوننت‌های Accessible
- Streaming: Server-Sent Events
- Validation: Zod
- Search: Meilisearch با استفاده از الگوی indexer موجود در مخزن رسمی
- تاریخچه: IndexedDB
- مدل: Gemini 3.5 Flash-Lite از طریق Adapter قابل‌تعویض
- Sanitization: کتابخانه تشخیص Secret به‌همراه Ruleهای اختصاصی
- Observability: Structured JSON logs و OpenTelemetry یا سرویس هم‌ارز
- Container: Docker چندمرحله‌ای با کاربر non-root

### ۱۴.۳ چرا Meilisearch

مخزن رسمی لیارا هم‌اکنون Crawler، synonym، stop-word و Schema مناسب Meilisearch دارد. استفاده از همان الگو:

- ریسک ناسازگاری با ساختار داک را کم می‌کند.
- جستجوی فارسی و Keywordهای فنی را حفظ می‌کند.
- URL، title، element و platform را برای Citation فراهم می‌کند.
- امکان تحویل سریع‌تر یک Retrieval قابل‌توضیح را می‌دهد.

وابستگی مستقیم به endpoint عمومی search.liara.ir توصیه نمی‌شود؛ ایندکس محصول باید مستقل و قابل نسخه‌بندی باشد.

## ۱۵. Pipeline مستندات و Retrieval

### ۱۵.۱ Ingestion

1. خواندن public/llms/**/*.md
2. استخراج Original link، عنوان، Heading، Breadcrumb و سرویس
3. حذف Boilerplateهای تکراری مانند all links
4. Chunk کردن بر اساس Heading و مرز معنایی
5. حفظ بلوک کد همراه پاراگراف توضیحی آن
6. تولید content hash برای تشخیص تغییر
7. ایندکس هر Chunk با metadata

### ۱۵.۲ Metadata هر Chunk

- doc_id
- chunk_id
- title
- service
- breadcrumb
- heading
- canonical_url
- content
- content_hash
- source_commit
- indexed_at
- language
- code_languages

### ۱۵.۳ نرمال‌سازی فارسی

- یکسان‌سازی ی و ک فارسی/عربی
- حذف فاصله و نیم‌فاصله اضافی
- حفظ Keywordهای انگلیسی، نسخه و Error code
- استفاده از synonymهای موجود در مخزن
- عدم Stem کردن نام محصول، دستور و نام پکیج

### ۱۵.۴ Retrieval

- برای سؤال ساده: یک Query و دریافت Top 8
- برای سؤال پیچیده: تولید حداکثر ۳ Query هدفمند و ادغام نتایج
- تنوع نتایج بر اساس سند و Heading
- ارسال حداکثر ۶ تا ۸ Chunk نهایی به مدل
- عدم ارسال کل corpus یا کل صفحه
- Citation فقط از Chunkهای نهایی

### ۱۵.۵ به‌روزرسانی

- ایندکس باید source commit و زمان به‌روزرسانی را ثبت کند.
- Sync خودکار مخزن حداقل روزانه یا در زمان Build انجام شود.
- Build ایندکس جدید به‌صورت atomic جایگزین نسخه قبلی شود.
- در شکست Sync، نسخه سالم قبلی فعال بماند.

## ۱۶. Prompt و کنترل مدل

### ۱۶.۱ مدل

مدل اصلی Gemini 3.5 Flash-Lite است. نام مدل، Base URL و API Key فقط از Environment خوانده می‌شوند و Adapter باید امکان تغییر Provider را بدون تغییر منطق محصول فراهم کند.

### ۱۶.۲ قواعد Prompt

- پاسخ همیشه فارسی است.
- ادعای Liara بدون Citation ممنوع است.
- مدل باید میان «اطلاعات سند»، «داده کاربر» و «دانش عمومی» تفکیک کند.
- مدل نباید از Citation جعلی یا URL ساخته‌شده استفاده کند.
- اگر سؤال مبهم است، پاسخ طولانی تولید نکند و یک سؤال بپرسد.
- اگر Confidence پایین است، حدس نزند.
- سطح فنی را در متن نهایی نام‌گذاری نکند.
- دستور خطرناک یا تخریبی باید هشدار و مرحله تأیید داشته باشد.
- Secret ماسک‌شده نباید بازسازی شود.

### ۱۶.۳ بودجه Token

- Context گفتگو با Summary کنترل شود.
- فقط پیام‌های مرتبط اخیر و خلاصه وضعیت Agent ارسال شوند.
- Chunkهای بازیابی‌شده محدود و Deduplicate شوند.
- خروجی پیش‌فرض کوتاه و قابل‌گسترش باشد.
- برای هر پیام عادی حداکثر یک Call مولد هدف است.
- Call اضافه فقط برای Query expansion پیچیده یا Repair خروجی مجاز است.

## ۱۷. امنیت و حریم خصوصی

### ۱۷.۱ Secret Redaction

ماسک‌کردن در دو لایه انجام می‌شود:

1. Client پیش از ارسال
2. Server پیش از ساخت Prompt

موارد حداقلی:

- API Key و Token
- Authorization و Bearer header
- Password و Secret در ENV
- Database connection string
- Private key
- Cookie و Session ID
- Access key و Secret key

جایگزین به‌شکل [SECRET_1] انجام می‌شود. تعداد موارد ماسک‌شده به کاربر اعلام می‌شود، اما مقدار قابل Reveal نیست.

### ۱۷.۲ ذخیره‌سازی و Logging

- Raw prompt و Raw attachment در لاگ ذخیره نشود.
- نسخه خام فقط در حافظه و تا پایان درخواست وجود داشته باشد.
- تاریخچه محلی فقط نسخه پاک‌سازی‌شده را نگه دارد.
- Metricها با شناسه تصادفی و بدون PII ثبت شوند.
- API Key مدل فقط Server-side باشد.

### ۱۷.۳ امنیت ورودی و خروجی

- MIME واقعی فایل بررسی شود.
- فایل اجرا یا unzip نشود.
- Markdown و HTML پاسخ sanitize شوند.
- CSP، HSTS، X-Content-Type-Options و Referrer-Policy فعال باشند.
- CORS فقط Originهای موردنیاز را بپذیرد.
- Fetch URL دلخواه و SSRF ممنوع باشد.
- حداکثر اندازه Body و Timeout اعمال شود.

### ۱۷.۴ Rate Limiting

پیشنهاد اولیه برای کاربر ناشناس:

- ۱۲ پیام در دقیقه به‌ازای IP و Session
- ۶۰ پیام در ساعت
- ۵ درخواست فایل در دقیقه
- Burst کوچک برای Streaming مجاز
- پاسخ 429 با زمان تلاش مجدد و حفظ Draft

مقادیر باید از Environment قابل تنظیم باشند.

## ۱۸. پایداری و Failure Handling

| Failure | رفتار محصول |
|---|---|
| Timeout مدل | توقف محترمانه، حفظ پیام و دکمه تلاش مجدد |
| خطای 429 مدل | Backoff با Jitter و پیام زمان‌بندی‌شده |
| خروجی Schema نامعتبر | یک Repair attempt، سپس پاسخ کنترل‌شده |
| Retriever از دسترس خارج | نمایش نتایج Source-only در صورت امکان؛ عدم پاسخ ساختگی |
| Citation نامعتبر | حذف ادعای بدون منبع یا کاهش Confidence |
| قطع Stream | نگهداری بخش دریافت‌شده و امکان ادامه |
| فایل نامعتبر | توضیح دقیق نوع یا اندازه مجاز |
| Sync داک ناموفق | ادامه سرویس با آخرین ایندکس سالم |

### ۱۸.۱ Retry

- حداکثر ۲ Retry برای خطاهای موقت
- عدم Retry برای 4xx به‌جز 429
- Idempotency key برای جلوگیری از پاسخ تکراری
- Circuit breaker برای خطاهای متوالی Provider

### ۱۸.۲ Health

- /healthz برای زنده‌بودن Process
- /readyz برای آمادگی Retriever و تنظیم Provider
- /api/docs/status برای نسخه ایندکس و source commit

## ۱۹. Monitoring و Observability

### ۱۹.۱ Structured Log

هر Request فقط metadata زیر را ثبت می‌کند:

- request_id
- anonymous_session_id هش‌شده
- timestamp
- intent
- service
- model
- latency_to_first_token
- total_latency
- input/output token estimate
- retrieved_chunk_ids
- citation_count
- confidence
- status_code
- retry_count
- escalation_created
- تعداد Secretهای ماسک‌شده، نه مقدار آن‌ها

### ۱۹.۲ Metricهای اصلی

- Request count و Error rate
- p50 و p95 زمان اولین Token
- p50 و p95 زمان پاسخ کامل
- Token به‌ازای پاسخ
- تعداد Call مدل به‌ازای پیام
- Cache hit rate
- Retrieval no-result rate
- Citation validation failure
- Clarification rate
- Escalation rate
- Like/Dislike rate
- Secret redaction count

### ۱۹.۳ Alertهای پیشنهادی

- Error rate بیشتر از ۵٪ در ۵ دقیقه
- p95 پاسخ ساده بیشتر از ۱۵ ثانیه
- افزایش ناگهانی 429
- Citation failure بیشتر از ۲٪
- شکست Sync مستندات
- عبور مصرف روزانه Token از بودجه

## ۲۰. بهینه‌سازی هزینه

- استفاده از Gemini 3.5 Flash-Lite
- یک Call مولد برای مسیر عادی
- Retrieval قبل از LLM و ارسال فقط Chunkهای لازم
- Cache پاسخ برای Queryهای عمومی و بدون داده شخصی
- Cache نتیجه Retrieval بر اساس Query نرمال‌شده
- خلاصه‌سازی Context طولانی و حذف پیام‌های نامرتبط
- عدم ارسال دوباره Attachment کامل در هر Turn
- استخراج بخش مرتبط لاگ پیش از Prompt
- محدودیت Output و پاسخ Progressive
- Batch کردن Ingestion و Sync اسناد
- ثبت هزینه تخمینی هر Request در Metricها

هدف اولیه در تعرفه استاندارد عمومی گوگل: میانگین هزینه کمتر از ۰٫۰۱ دلار برای یک Turn عادی مستندمحور.

## ۲۱. APIهای منطقی

| Endpoint | کاربرد |
|---|---|
| POST /api/chat | دریافت پیام پاک‌سازی‌شده و Stream پاسخ |
| POST /api/attachments/inspect | اعتبارسنجی، استخراج و ماسک فایل |
| GET /api/sources/:chunkId | دریافت metadata و Snippet منبع |
| POST /api/feedback | ثبت پسند، نپسندیدن و دلیل اختیاری |
| GET /api/docs/status | نسخه و زمان ایندکس |
| GET /healthz | Liveness |
| GET /readyz | Readiness |

## ۲۲. معیارهای پذیرش کلیدی

### AC-01: Citation

اگر پاسخ شامل ادعایی درباره رفتار لیارا باشد، Citation بلافاصله بعد از ادعا نمایش داده می‌شود و به URL رسمی و Heading مرتبط می‌رسد.

### AC-02: سؤال مبهم

اگر نوع سرویس یا مرحله خطا مشخص نباشد، عامل به‌جای فهرست‌کردن راه‌حل‌های تصادفی، یک سؤال تکمیلی با بیشترین ارزش اطلاعاتی می‌پرسد.

### AC-03: تغییر سطح تخصص

اگر پیام اول ساده و پیام بعدی فنی باشد، پاسخ دوم بدون نمایش برچسب سطح، متناسب با جزئیات فنی پیام دوم تولید می‌شود و Context مسئله حفظ می‌گردد.

### AC-04: Scroll

اگر کاربر بیش از یک Viewport از انتهای گفتگو فاصله داشته باشد، ورود پاسخ جدید موقعیت او را تغییر نمی‌دهد و CTA پیام جدید ظاهر می‌شود.

### AC-05: Secret

اگر فایل ENV شامل کلید آزمایشی باشد، کلید پیش از Request مدل ماسک می‌شود، در UI تاریخچه دیده نمی‌شود و در Structured Log وجود ندارد.

### AC-06: پاسخ خارج از اسناد

اگر پاسخ Liara-specific در corpus وجود نداشته باشد، سیستم عدم قطعیت را اعلام و Escalation تولید می‌کند.

### AC-07: دانش عمومی

اگر تحلیل عمومی کد ارائه شود، بخش عمومی با برچسب مشخص از محتوای مستندشده لیارا جدا است.

### AC-08: تاریخچه

پس از Refresh، مکالمه پاک‌سازی‌شده و منابع آن روی همان دستگاه قابل ادامه است.

### AC-09: Responsive

در عرض‌های ۳۶۰، ۷۶۸ و ۱۴۴۰ پیکسل، Composer، کد، منابع و تاریخچه بدون Overflow مخرب قابل استفاده‌اند.

### AC-10: Failure

در Timeout مدل، Draft و پیام کاربر از بین نمی‌رود و Retry باعث ایجاد پیام تکراری نمی‌شود.

## ۲۳. Benchmark کیفیت

چون داده واقعی تیکت وجود ندارد، یک Golden Set نسخه‌بندی‌شده مستقیماً از corpus ساخته می‌شود.

### ۲۳.۱ ترکیب مجموعه

حداقل ۱۲۰ سناریو:

| نوع | تعداد |
|---|---:|
| سؤال مستقیم و تک‌سندی | ۳۵ |
| سؤال چندمرحله‌ای یا چندبخشی | ۲۵ |
| عیب‌یابی با لاگ، کد یا تنظیمات مصنوعی | ۲۰ |
| سؤال مبهم که نیازمند Clarification است | ۱۵ |
| سؤال فاقد پاسخ در داک | ۱۰ |
| سؤال دارای تعارض یا دام نسخه | ۵ |
| Prompt injection و ورودی حساس | ۱۰ |

همه سرویس‌های اصلی باید حداقل پنج سناریو داشته باشند و سهم PaaS، DBaaS، One Click و AI به‌دلیل حجم بالاتر مستندات بیشتر باشد.

### ۲۳.۲ ساخت Golden Answer

هر سناریو شامل موارد زیر است:

- سؤال
- Context یا فایل آزمایشی
- Intent مورد انتظار
- اسناد و Headingهای معتبر
- نکات الزامی پاسخ
- ادعاهای ممنوع
- نیاز یا عدم نیاز به سؤال تکمیلی
- Confidence مورد انتظار
- رفتار Escalation

### ۲۳.۳ Metricهای پذیرش

| Metric | هدف |
|---|---:|
| صحت نکات الزامی | حداقل ۹۰٪ |
| Citation precision | حداقل ۹۵٪ |
| Citation coverage ادعاهای Liara-specific | حداقل ۹۵٪ |
| URL معتبر منابع | ۱۰۰٪ |
| امتناع صحیح در سؤال بدون پاسخ | حداقل ۹۰٪ |
| سؤال تکمیلی مناسب | حداقل ۸۵٪ |
| نشت Secret در تست‌های Seeded | صفر |
| تکمیل موفق Flow عیب‌یابی | حداقل ۸۰٪ |
| پاسخ خارج از Scope بدون Grounding | کمتر از ۲٪ |

## ۲۴. نگاشت به معیارهای داوری

### ۲۴.۱ کیفیت و صحت پاسخ‌ها — ۸۰ امتیاز

شواهد محصول:

- Retrieval از corpus رسمی
- Citation در سطح ادعا
- Citation validator
- Confidence و Abstention
- Golden Set ساده، پیچیده و Unanswerable
- جداسازی دانش عمومی
- Escalation به‌جای Hallucination

### ۲۴.۲ طراحی UI و تجربه کاربری — ۵۵ امتیاز

شواهد محصول:

- رابط فارسی RTL و Responsive
- Streaming و Stop/Retry
- نمایش حرفه‌ای کد، لینک و Artifact
- پنل منبع شبیه ChatGPT
- تاریخچه محلی و ادامه Conversation
- رفتار Scroll تطبیقی
- Empty state، Prompt پیشنهادی، Mobile sheet و Accessibility

### ۲۴.۳ Agentic و Personalization — ۵۰ امتیاز

شواهد محصول:

- تشخیص Intent
- سطح فنی جداگانه برای هر پیام
- سؤال تکمیلی هدفمند
- State عیب‌یابی چندمرحله‌ای
- حفظ اقدام‌های انجام‌شده
- تولید Artifact
- پیشنهاد قدم بعدی
- Escalation ساختاریافته

### ۲۴.۴ امنیت، پایداری و Monitoring — ۵۰ امتیاز

شواهد محصول:

- Redaction دو‌لایه
- عدم ذخیره Raw prompt
- Rate limit و Token budget
- Retry، Timeout، Circuit breaker و Idempotency
- Structured logging بدون محتوای حساس
- Metric، Alert و Health endpoint
- Citation و Schema validation

### ۲۴.۵ استقرار روی لیارا — ۴۰ امتیاز

شواهد موردنیاز:

- Dockerfile چندمرحله‌ای
- کاربر non-root
- Health و Readiness
- Environment نمونه بدون Secret
- Build تکرارپذیر
- Migration یا Index build مشخص
- README استقرار، Rollback و Smoke test
- لینک نسخه Deploy شده

### ۲۴.۶ بهینه‌سازی هزینه — ۲۵ امتیاز

شواهد محصول:

- Flash-Lite
- Retrieval پیش از LLM
- حداکثر Call و Token budget
- Cache و Context summary
- Metric هزینه
- عدم ارسال مجدد فایل کامل
- هدف هزینه به‌ازای Turn

## ۲۵. اولویت‌بندی مبتنی بر امتیاز

### P0 — هسته قابل داوری

- RAG و Citation دقیق
- چت Streaming فارسی
- پنل منابع
- Intent، Clarification و Context
- تطبیق سطح فنی در هر پیام
- عیب‌یابی چندمرحله‌ای
- ورودی فایل و Secret masking
- تولید Artifact و Escalation
- تاریخچه محلی و Scroll تطبیقی
- Rate limit، Error handling، Log و Health
- Docker و تنظیمات استقرار

### P1 — تقویت امتیاز و نگهداشت

- Feedback و گزارش کیفیت
- Dark mode
- جستجو و Export تاریخچه
- Sync خودکار و atomic داک
- Dashboard Metric
- Cache پیشرفته

### P2 — توسعه آینده

- حساب کاربری و Sync چنددستگاهی
- اتصال Read-only به حساب لیارا با مجوز رسمی
- دریافت مستقیم لاگ با Consent
- ارسال واقعی تیکت
- پنل مدیریت Golden Set
- Action execution با Preview و تأیید

## ۲۶. سناریوی دموی داوری

دمو باید در کمتر از ۶ دقیقه شواهد همه معیارها را نمایش دهد:

1. **Empty state و Responsive:** نمایش Desktop و Mobile.
2. **پاسخ مستند:** پرسش درباره یک قابلیت و بازکردن Citation دقیق.
3. **کد و لینک:** نمایش بلوک کد با Copy و منبع.
4. **عیب‌یابی:** ارسال لاگ و فایل تنظیمات، سپس پاسخ به سؤال تکمیلی عامل.
5. **Personalization:** ادامه همان گفتگو با یک پیام بسیار فنی و مشاهده تغییر عمق پاسخ بدون برچسب سطح.
6. **امنیت:** ورود یک Secret آزمایشی و نمایش پیام ماسک‌شدن.
7. **Agentic flow:** اجرای چند مرحله تا تولید Checklist یا Artifact.
8. **عدم Hallucination:** پرسش فاقد پاسخ و ساخت تیکت Escalation.
9. **UX Scroll:** بالا رفتن در تاریخچه و دریافت پاسخ جدید بدون پرش.
10. **Persistence:** Refresh و بازیابی Conversation.
11. **Monitoring:** نمایش Health، request_id و Metricهای بدون داده خام.
12. **هزینه و Deploy:** نمایش Token budget، Docker و URL نسخه لیارا.

## ۲۷. رویدادهای Analytics

بدون ذخیره متن پیام:

- conversation_started
- message_submitted
- attachment_added
- secret_redacted
- clarification_asked
- source_opened
- code_copied
- next_action_selected
- response_stopped
- response_retried
- feedback_submitted
- escalation_created
- conversation_deleted

هر Event فقط شناسه ناشناس، timestamp، نوع سرویس و metadata غیرحساس دارد.

## ۲۸. ریسک‌ها و راهکارها

| ریسک | اثر | راهکار |
|---|---|---|
| توان استدلالی محدود Flash-Lite | پاسخ پیچیده ناقص | Flow ساختاریافته، Retrieval قوی، سؤال تکمیلی و Golden eval |
| کیفیت جستجوی فارسی | سند اشتباه | نرمال‌سازی، synonym، Keyword انگلیسی و تست Retrieval |
| داک قدیمی یا تغییر URL | Citation خراب | Sync نسخه‌بندی‌شده و Link checker |
| لاگ بسیار بلند | هزینه و Context بالا | استخراج خطا، Windowing و ارسال بخش مرتبط |
| Prompt injection در فایل | نقض سیاست | جداسازی داده، allowlist Citation و validator |
| Secret ناشناخته | نشت داده | Redaction دو‌لایه، عدم Logging و تست Seeded |
| پاسخ عمومی به‌جای Liara-specific | کاهش صحت | برچسب عمومی و الزام Citation |
| Provider outage | قطع تجربه | Retry، Circuit breaker و Source-only fallback |
| تاریخچه محلی حذف شود | از دست رفتن گفتگو | Export اختیاری و توضیح شفاف محدودیت |

## ۲۹. فرض‌های قطعی

- محصول فقط از داک رسمی و ورودی آگاهانه کاربر استفاده می‌کند.
- رابط و پاسخ‌ها فارسی هستند.
- کد و محتوای فنی LTR باقی می‌مانند.
- سطح تخصص هر پیام جداگانه و مخفیانه تشخیص داده می‌شود.
- Gemini 3.5 Flash-Lite مدل اصلی است.
- استقرار نهایی توسط تیم پروژه انجام می‌شود.
- داده واقعی تیکت موجود نیست.
- تاریخچه پاک‌سازی‌شده فقط روی دستگاه ذخیره می‌شود.
- ارسال یا اجرای Action واقعی خارج از دامنه است.

## ۳۰. Definition of Done

محصول وقتی آماده ارزیابی است که:

- تمام الزامات P0 پیاده‌سازی و Smoke test شده باشند.
- Golden Set نسخه‌بندی‌شده اجرا و Metricهای حداقلی ثبت شده باشند.
- هیچ Secret آزمایشی در Prompt ثبت‌شده، History یا Log نشت نکرده باشد.
- Citationهای سناریوهای دمو به Heading صحیح برسند.
- Mobile، Tablet و Desktop بررسی شده باشند.
- Failureهای 429، Timeout، قطع Stream و Retriever شبیه‌سازی شده باشند.
- Docker image بدون Secret ساخته شود و با کاربر non-root اجرا گردد.
- Health و Readiness سبز باشند.
- README شامل Run محلی، Environment، Indexing، تست و Deploy باشد.
- نسخه نهایی روی لیارا قابل دسترسی و لینک GitHub آماده ارائه باشد.
